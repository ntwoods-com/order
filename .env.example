# Copy to .env for local dev ONLY (do not commit real secrets)
# For PythonAnywhere: set these in Web tab -> Environment variables

APP_ENV=production
SECRET_KEY=change-me-to-a-long-random-string

# JWT Secret for API authentication (can be same as SECRET_KEY)
JWT_SECRET=change-me-to-another-long-random-string

# Format: USER1=username:bcrypt_hash
# Generate bcrypt hash with a small helper script (see README)
USER1=admin:$2b$12$replace_with_bcrypt_hash
USER2=user2:$2b$12$replace_with_bcrypt_hash

# Admin users (comma-separated usernames who can access /admin)
ADMIN_USERS=admin

# Optional
# CORS for React frontend (GitHub Pages). If not set, API allows all origins ("*").
# CORS_ALLOWED_ORIGINS=https://<username>.github.io,https://<custom-domain>

# JWT expiry in seconds (default: 28800 = 8 hours)
# JWT_EXPIRES_SECONDS=28800

# Production database (recommended for Render)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
# DB_SSLMODE=require
# DATABASE_FILE=/home/<your-username>/<your-project>/order_counter.db
# UPLOAD_FOLDER=/home/<your-username>/<your-project>/uploads
# REPORT_FOLDER=/home/<your-username>/<your-project>/reports
# SESSION_COOKIE_SECURE=1
# LOG_FILE=/home/<your-username>/logs/app.log
